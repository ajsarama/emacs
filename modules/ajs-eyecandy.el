;;; This file includes configuration for:
;; Spacious padding
;; Modeline

(use-package spacious-padding
  :demand t
  :config
  (setq spacious-padding-subtle-mode-line t)
  (spacious-padding-mode 1))

(provide 'ajs-eyecandy)
